{
  "name": "oscar-odds",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "tsx server.ts",
    "dev": "tsx server.ts",
    "smoke": "sh -c 'tsx server.ts & PID=$!; trap \"kill $PID 2>/dev/null || true\" EXIT; sleep 2; curl -fsS -i http://localhost:3000/api/profiles'",
    "scrape:sources": "tsx scripts/poll-sources.ts --once",
    "poll:sources": "tsx scripts/poll-sources.ts --interval-minutes 30",
    "typecheck": "tsc --noEmit",
    "build:client": "esbuild app.ts --bundle --format=esm --platform=browser --target=es2022 --outfile=app.js",
    "build": "npm run build:client",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "pretest:e2e": "npm run build:client",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {
    "better-sqlite3": "^12.6.2",
    "cheerio": "^1.0.0",
    "express": "^4.21.2",
    "express-rate-limit": "^8.2.1",
    "js-yaml": "^4.1.1",
    "pino": "^10.3.1",
    "swagger-ui-express": "^5.0.1",
    "tsx": "^4.21.0",
    "zod": "^4.3.6",
    "lz-string": "^1.5.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.58.2",
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^25.3.0",
    "@types/swagger-ui-express": "^4.1.8",
    "@vitest/coverage-v8": "^3.2.4",
    "esbuild": "^0.27.3",
    "eslint": "^10.0.1",
    "globals": "^17.3.0",
    "pino-pretty": "^13.1.3",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.0",
    "vitest": "^3.2.4"
  }
}
